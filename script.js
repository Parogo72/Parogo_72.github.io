window.addEventListener('resize', () => {
  if(window.innerHeight < window.innerWidth && window.innerWidth < 650){
      alert("Utiliza la pantalla en vertical para disfrutar al máximo de la página"); 
 }
});
